[tool.poetry]
name = "shift-ml-testtask"
version = "0.1.0"
description = ""
authors = ["nnnLik <makhmudov_ra@vk.com>"]
readme = "README.md"
packages = [{include = "shift_ml_testtask"}]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.97.0"
fastapi-users = {extras = ["sqlalchemy"], version = "^11.0.0"}
sqlalchemy = "^2.0.16"
asyncpg = "^0.27.0"
alembic = "^1.11.1"
python-decouple = "^3.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
